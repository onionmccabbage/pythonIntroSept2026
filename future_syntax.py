# in the future, Python will have tools for design time type casting

# in this example, we indicate that the function must return a string
def showDemo()->str:
    # return 'hello world'
    return True # the code does NOT enforce a string return

print( showDemo() ) 